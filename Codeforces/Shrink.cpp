#include<bits/stdc++.h>
using namespace std;


void solve(){
    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n;

    long long t; cin>>t;
    while(t--) 
    
    {
        
        cin>>n;
        vector<int> final(n);
        // final.push_back(2);
        cout<<2<<" ";
        for (int i = 0; i < n-2; i++)
        {
            cout<<n-i<<" ";
            
        }
        cout<<1<<endl;
        
    }

    // solve();
    return 0;
}